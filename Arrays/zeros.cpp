#include<iostream>
#include<vector>
using namespace std;
int main(){
    //get in the loop;
    //check zeros at first and last indices
    //if present skip the iteration and increase and decrease its indices
    //else count no. of zeros :

    vector<int> height = {0,0,0,1,0,3,3,2,0,0,7,6,0,4,0,8,0};
    int count = 0;
    int n = height.size();
    for(int i=0;i<n;i++){
        if(height[])
    }

}